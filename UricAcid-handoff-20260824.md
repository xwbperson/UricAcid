# UricAcid 项目交接摘要

> 生成时间：2026-08-24  
> 用途：下一次会话继续实现 UricAcid 网页应用  
> 敏感信息：共享访问口令已脱敏，禁止从本文件推断或复原

> 状态更新：以下“实现前基线”保留用于追溯；代码实现、自动化证据和未完成外部验收以 [IMPLEMENTATION_STATUS.md](D:/Project/ProjectsActive/UricAcid/IMPLEMENTATION_STATUS.md) 为准。

## 下一次会话目标

实现并验证手机优先的高尿酸饮食记录 Web 应用候选 v1；继续完成真实目标服务器、参考数据人工核验和用户验收。

## 当前工作区事实

- 工作目录：`D:\Project\ProjectsActive\UricAcid`
- 生成本交接时的唯一项目文件：[PROJECT_SPEC.md](D:/Project/ProjectsActive/UricAcid/PROJECT_SPEC.md)；当前工作区已包含实现、测试和 Docker 配置。
- 当前已初始化 Git，远端为 `https://github.com/xwbperson/UricAcid`，交付分支固定为 `main`。
- 规格状态仍为 `PREPARED`；实现状态和外部验收边界见 `IMPLEMENTATION_STATUS.md`，首批种子数据仍不是 `VERIFIED`。
- 规格已经过独立读者复核；范围单位、尿酸统计、移动端路径、备份迁移、共享口令会话规则没有遗留 P0/P1。

## 已确认的产品决策

### 医学/计算口径

- 不计算“每种食物使个人血尿酸升高多少 μmol/L”。
- 食物参考值：`mg/100g（可食部）`。
- 菜谱参考值：`mg/100g（成品）`。
- 实际摄入贡献：按克数换算为 `mg`。
- 例如 `10–20` 显示为 `10–20 mg/100g`；`10–10` 显示为“约 10 mg/100g”。
- 当日结果是膳食嘌呤估算范围，不是血尿酸值；饮品量不抵扣嘌呤。
- 血尿酸实测是独立记录，支持日期、数值、`μmol/L`/`mg/dL`、可选检测条件和统计趋势。

### 功能范围

- 今日页：食物/菜谱、饮品、血尿酸三个快速入口。
- 食物、菜谱、饮品都支持自定义分组和无分组。
- 管理页按食物、菜谱、饮品标签分别管理自己的分组，不提供汇总的独立“分组”标签；删除分组只会把该标签页下的内容移到未分组。
- 食物、菜谱、饮品参考库条目可从可选库删除（归档，保留历史快照）；每日饮食、饮品和尿酸记录可逐条删除。
- 食物/菜谱支持搜索选择器、克数模板和自定义克数。
- 饮品预置：纯净水、无糖苏打水、淡绿茶、无糖柠檬水、黑咖啡/无糖咖啡、无糖薏仁水、无糖大麦茶、无糖冬瓜茶、无糖玉米须茶；“系统预置”不等于医学推荐。
- 菜谱支持按配料计算和手工范围；配料计算需成品重量；历史条目保存参考值快照。
- 历史页、统计页、资料管理、来源登记、份量模板、JSON/ZIP 导出、CSV 辅助导出、恢复预览和迁移。

### 访问方式（重要）

- 不创建账户、用户名或新用户。
- 使用一个共享访问口令；明文初始口令为 `[REDACTED]`，不得写入文档、源码、Git、数据库导出或日志。
- 新设备/新浏览器首次输入一次口令，默认信任设备 365 天；清除 Cookie、无痕模式、换设备、过期、改口令或撤销后重新输入。
- 浏览器保存随机 `HttpOnly + Secure + SameSite` Cookie；服务端保存凭证哈希。
- 每个受保护页面/API 请求都查询服务端凭证记录并检查：存在、未撤销、未过期、会话世代有效；不能只验证离线 JWT。
- 共享口令用现代内存困难哈希（当前实现为 scrypt）保存于服务器私密配置，不放业务数据库。初始化工具应交互式读取口令，不从命令行参数读取。
- 修改口令或服务器重设口令要立即撤销全部可信设备。
- 应用使用自有域名 HTTPS；局域网/VPN/IP 白名单是可选的额外边界，不替代共享口令。

## 技术默认

- 功能模块化单体：`access_gate`、`food`、`recipe`、`beverage`、`entry`、`measurement`、`statistics`、`settings`、`backup`。
- 单实例 SQLite，数据目录挂载持久化卷；未来多空间/多实例再评估 PostgreSQL。
- Docker Compose + HTTPS 反向代理。
- 业务表属于一个数据空间，不使用 `user_id`、账户或角色。
- 关键实体见规格第 11 节；包括 `app_settings`、`trusted_device_sessions`、食物/菜谱版本、饮食/饮品条目、尿酸实测、备份记录。
- SQLite 快照或导出恢复前清空全部 `trusted_device_sessions` 并轮换会话世代，旧设备不能复活。
- 可移植导出不包含共享口令/哈希、设备凭证、签名密钥、备份密钥；目标服务器先配置口令哈希和新签名密钥，迁移后设备重新验证一次。

## 计算与测试重点

```text
entry_low  = quantity_g / basis_g * purine_low
entry_high = quantity_g / basis_g * purine_high
daily_low  = sum(known_entry_low)
daily_high = sum(known_entry_high)
μmol/L = mg/dL * 59.48
```

- 未知参考值不能按 0 计算；当天显示“部分覆盖”。
- 尿酸统计先统一为 `μmol/L` 计算，再按首选单位显示；原始单位和值仍保留。
- 手机最窄约 360px；快捷录入、弱网重试幂等、编辑删除、图表下数据表均需测试。

## 医学资料位置

权威来源、数据建库要求和医学免责声明已经写入 `PROJECT_SPEC.md` 第 3、8 节，不要在交接文件中重复复制数据表。首批种子数据仍需逐条人工核对来源、名称、品种、生熟/干湿状态和单位后才可标记 `VERIFIED`。

## 下一步建议

1. 选定实现栈并初始化应用；先搭建访问口令、可信设备 Cookie 和 SQLite 迁移。
2. 实现食物/菜谱/饮品/尿酸数据模型与服务端计算，补充单元测试。
3. 实现手机优先今日页和三个快速录入流程，再实现历史、统计和管理页。
4. 导入少量已人工核验的参考数据，验证 `10–20`、`10–10`、菜谱成品重量和未知覆盖规则。
5. 实现 JSON/ZIP 导出、SQLite 安全快照、恢复前清除可信设备、异机复制和恢复演练。
6. 在目标服务器以 HTTPS 部署，完成手机/电脑回归和共享口令生命周期验收。

## 当前尚未完成/不可声称的事项

- 本地候选实现已通过自动化测试，但没有真实目标服务器证据，不能声称 HTTPS、容器重建持久化、异机迁移或空环境恢复已完成。
- 文档中的首批示例种子数据仍需逐条核对名称、状态、单位和来源页码，不能标为 `VERIFIED`。
- 主要手机、偶用电脑和真实部署环境的用户验收尚未完成，不能标为 `ACCEPTED`。
- 不能声称对血尿酸有预测能力或医学有效性。

## Suggested skills

- `implement`：根据规格按垂直切片实现 v1。
- `webapp-testing`：用手机视口和浏览器回归录入、统计、访问口令和恢复流程。
- `frontend-design`：实现移动优先、低密度录入和可读统计界面。
- `security-best-practices`：审查共享口令、长期 Cookie、CSRF、备份密钥和公网部署边界。
- `spreadsheets`：如果把参考库维护为 CSV/XLSX，按数据来源、单位和版本做导入校验。
