# UricAcid 高尿酸饮食与尿酸趋势记录系统

> 文档版本：v0.2
> 日期：2026-08-25
> 状态：`IMPLEMENTED CANDIDATE`（本地实现与自动化验证已完成；尚未部署或由用户验收）
> 目标形态：手机优先、电脑兼容、可部署到私人服务器的单数据空间 Web 应用

## 1. 项目结论

本项目用于持续记录饮食、饮品和血尿酸实测数据，帮助用户回看自己的长期趋势。它不是医疗诊断或治疗系统。

原规划中的“每 100g 食物预计提高多少血尿酸”需要改名并改变计算口径。当前没有可靠的通用公式，可以把一份食物换算成某个人血尿酸必然升高多少 `μmol/L`。v1 应严格分开两类数据：

1. **估算膳食嘌呤摄入范围**：单位为 `mg`，根据食物参考数据和实际克数计算。
2. **血尿酸实测值**：单位为 `μmol/L` 或 `mg/dL`，只来自用户录入的医院、体检或家用设备结果。

饮水量单独统计，不从嘌呤摄入中抵扣，也不换算成“降低了多少尿酸”。统计页可以把同期数据并列展示，但不能把相关性写成因果关系。

推荐的 v1 决策如下：

| 决策项 | v1 方案 |
| --- | --- |
| 核心汇总 | 估算膳食嘌呤摄入范围，不显示“预计血尿酸升高值” |
| 访问方式 | 无账户和用户名；新设备首次输入共享访问口令，之后长期直接打开记录 |
| 主要终端 | 手机优先，电脑完整兼容 |
| 食物与菜谱 | 均支持分组和无分组；按克数换算 |
| 菜谱计算 | 同时支持“按配料计算”和“手工范围” |
| 饮品模块 | 统计毫升数及类型，不计算治疗效果 |
| 尿酸记录 | 低频独立入口，可补录日期，统计页显示真实测量趋势 |
| 治疗记录 | 独立时间线记录发作、检查、用药、症状变化和复诊节点；支持同日多条记录及按类型筛选，不评价疗效 |
| 历史计算 | 保存录入时快照，不随参考表更新而静默变化 |
| 数据库 | 单实例 SQLite，数据目录挂载为服务器持久化卷 |
| 备份迁移 | 数据库安全快照 + 可移植 JSON 导出；必须通过恢复演练 |

## 2. 项目目标与非目标

### 2.1 v1 目标

- 在手机上快速记录当天吃过的食物或菜谱及其克数。
- 使用可搜索下拉选择器和常用份量模板，减少页面占用与输入次数。
- 快速记录各类饮品的毫升数。
- 每次保存、编辑或删除后，立即更新当天嘌呤估算范围和饮品总量。
- 支持低频补录血尿酸实测值，并查看长期趋势。
- 按时间线记录痛风发作、医院检查、口服药、外用药、症状变化和复诊计划等治疗过程节点；同一天允许添加多条，并可按事件类型筛选回看。
- 管理食物、菜谱、饮品、分组、份量模板和参考来源。
- 数据在服务器中持久保存，可自动备份、手动导出并迁移到新服务器。
- 每项估算都能追溯到数据来源、计算版本和录入时快照。

### 2.2 v1 非目标

- 不诊断高尿酸血症或痛风。
- 可以记录已经发生或计划中的用药、检查和症状事件，但不提供用药、停药、剂量调整、疗效或治疗建议。
- 不把单项食物换算成个人血尿酸浓度变化。
- 不用饮水量“抵消”饮食嘌呤负荷。
- 不预测下一次血尿酸值，不给出因果或相关性结论。
- 不自动生成所谓“降尿酸餐谱”。
- 不自动解读检查结果，不根据时间线推断某种药物是否有效。
- 不做多用户、家庭共享或医生端。
- 不做检查报告图片识别、条码扫描、离线 PWA 和外部消息提醒；今日页可以提供基于已记录数据的页面内进度/接近提示。

## 3. 医学与数据边界

### 3.1 为什么不能计算“预计升高多少血尿酸”

血尿酸同时受肾脏和肠道排泄、遗传、药物、体重、酒精、果糖、脱水、运动、疾病状态和测量时点等因素影响。食物总嘌呤本身也不能完整代表影响，不同嘌呤碱基、食物来源和烹饪状态的作用存在差异。

- [国家卫生健康委《成人高尿酸血症与痛风食养指南（2024年版）》](https://www.nhc.gov.cn/wjw/c100378/202402/afd5dda4bd6745fda10aad8d43a16369.shtml)强调个体化、均衡膳食，并明确食养不能代替药物治疗；建议每天至少摄入 `500g` 新鲜蔬菜，在心、肾功能正常且没有限液要求时足量、规律饮水 `2000–3000mL`。
- [国家卫生行业标准 WS/T 560—2017](https://www.nhc.gov.cn/wjw/yingyang/201708/93b17b29518447ccb194188ab9a7335b/files/1739783557085_80487.pdf)给出了部分常见食物的嘌呤含量和膳食原则，可作为中国食物种子数据来源之一；其定义中的高尿酸血症需要通常饮食状态下非同日两次空腹测量，不能用单次读数自动诊断。
- [美国风湿病学会 2020 痛风管理指南](https://pmc.ncbi.nlm.nih.gov/articles/PMC10563586/)对限制嘌呤、酒精和高果糖玉米糖浆的建议均为条件性建议，并指出饮食调整对血尿酸浓度的总体影响通常较小。
- [USDA 与 NIH-ODS 嘌呤数据库 Release 2.0（2025）](https://www.ars.usda.gov/northeast-area/beltsville-md-bhnrc/beltsville-human-nutrition-research-center/methods-and-application-of-food-composition-laboratory/mafcl-site-pages/purine-content-of-foods/)提供均值、最小值、最大值和来源信息，同时说明同种食物会因品种、烹饪状态和产品配方出现差异。
- [Kaneko 等 2014 年食物嘌呤分析](https://www.jstage.jst.go.jp/article/bpb/37/5/37_b13-00967/_pdf)以 HPLC 分析多类食物，说明总嘌呤、嘌呤碱基类型、干湿状态和实际食用量都需要区分。

因此，产品中的固定提示语为：

> 本工具估算的是膳食嘌呤摄入负荷，不是个人血尿酸升高值，也不用于诊断或治疗。血尿酸变化以实际检测和专业判断为准。

### 3.2 产品中的指标

| 指标 | 单位 | 来源 | 是否可相加 | 产品解释 |
| --- | --- | --- | --- | --- |
| 食物嘌呤参考范围 | mg/100g | 有来源的参考表 | 按克数换算后可相加 | 食物组成数据，不是血液浓度 |
| 单条嘌呤估算贡献 | mg | 参考范围 × 实际克数 | 是 | 本次记录的膳食负荷估算 |
| 当日嘌呤估算范围 | mg | 当天已知条目之和 | 是 | 同时显示数据覆盖情况 |
| 饮品摄入量 | mL | 用户记录 | 是 | 不抵扣嘌呤负荷 |
| 血尿酸实测值 | μmol/L 或 mg/dL | 用户录入的检测结果 | 否 | 独立的临床/设备测量 |

### 3.3 不制造虚假精度

- 食物统一使用 `mg/100g（可食部）`；菜谱统一使用 `mg/100g（成品）`。当天按实际克数换算后的摄入贡献使用 `mg`。
- 参考值以“下限 + 上限”保存。例如下限 10、上限 20，界面显示 `10–20 mg/100g`。
- 下限和上限允许相等。例如录入 `10–10` 时，内部保存 `[10, 10]`，界面显示“约 `10 mg/100g`”，不显示重复的 `10–10`。
- 当一条记录按克数换算后上下限相等，同样显示“约 `X mg`”；当天总计上下限相等时显示“今日约 `X mg`”。
- 不得为了满足“范围”格式，擅自给单点值增加 `±10%` 或其他人为误差。
- 只有在食物名称、品种、可食部、生熟状态和计量口径可比时，才可合并多个可信来源形成范围。
- 缺少参考值时允许记录食物，但显示“暂无估算”；未知值不能按 0 参与计算。
- 分类颜色只帮助浏览，不能替代完整饮食建议。

## 4. 访问与部署假设

v1 面向私人服务器上的一个共享数据空间：

- 不创建账户，不设置用户名，也不允许添加新用户。全站只有一个共享访问口令和一套数据。
- 新设备或新浏览器第一次访问时，只显示一个“访问口令”输入框；验证成功后签发可信设备凭证。
- 可信设备默认保持 `365 天`，期限可在服务器配置中调整。有效期内重新打开网页可直接查看和记录，无需再次输入口令。
- 清除浏览器数据、使用无痕模式、更换浏览器或设备、凭证到期、修改共享口令或主动撤销凭证后，需要重新输入一次口令。
- 浏览器只保存随机设备凭证，不保存共享口令。凭证放入 `HttpOnly + Secure + SameSite` Cookie，服务器只保存其哈希。
- 每次访问受保护页面或 API 时，服务端都用 Cookie 凭证哈希查询可信设备记录，并检查记录存在、未撤销、未过期且会话世代有效；不能只依赖一个可离线验证到期时间的长期 JWT。
- 共享口令使用 Argon2id 等现代算法生成哈希，并仅保存在服务器私密配置中；用户给出的明文初始口令不写入本文档、源码、Git、数据库导出或日志。
- 部署工具通过交互式标准输入接收初始口令并写入哈希，避免把明文放进命令行历史。缺少有效口令哈希时应用必须拒绝提供业务页面和 API，不能退化为公开访问或使用内置默认口令。
- 手机承担绝大多数日常录入；电脑主要用于批量管理、查看统计、备份和迁移。
- 默认时区为 `Asia/Shanghai`。
- 应用只运行一个实例；SQLite 使用持久化卷。若以后需要多数据空间、多实例或横向扩容，应重新设计隔离方式并考虑 PostgreSQL。
- 使用自有域名并启用 HTTPS。私人 VPN 或反向代理 IP 白名单可以作为可选的第二层保护，但不作为 v1 的必需交互。

## 5. 信息架构

移动端底部导航使用四个一级入口：

| 页面 | 核心职责 |
| --- | --- |
| 今日 | 记录食物、菜谱、饮品和尿酸；查看当天汇总 |
| 治疗记录 | 记录并按时间线回看治疗过程节点，支持日期范围和事件类型筛选 |
| 历史 | 查看和修改过去日期的记录 |
| 统计 | 查看尿酸实测、膳食负荷和饮品趋势 |
| 管理 | 管理资料库、分组、模板、设置、备份与迁移 |

电脑端沿用同一信息架构，可变为左侧导航和双栏布局，不建设第二套产品。

## 6. 手机优先交互规范

- 最窄按 `360px` 视口设计，不出现必须横向滚动的表单。
- 主要触控目标不小于约 `44×44px`，不能依赖悬停操作。
- “记录食物/菜谱”“记录饮品”“记录尿酸”“记录治疗”始终在今日页首屏可见。
- 食物和菜谱选择器使用可搜索的底部抽屉或全屏选择层，而不是狭小的原生下拉框。
- 分组用于筛选，最近使用和收藏项优先出现。
- 克数、毫升数使用数字键盘；快捷模板一触即选，自定义值无需进入管理页。
- 保存期间显示明确状态；服务器未确认前不得显示“已保存”。网络失败时保留表单内容并允许重试。
- 新建请求携带客户端唯一 ID 或幂等键，避免弱网重试和连续点击产生重复记录。
- 治疗记录的日期必填，事件时间可选；没有填写时间时只显示“时间未填写”，不得使用创建时间伪装成事件时间。
- 治疗时间线使用类型筛选器和日期范围筛选器；筛选后仍按事件日期和已填写的事件时间排序。
- 统计图下方提供数据表；图表不是读取数据的唯一方式。
- 管理页在手机上也必须可用，但允许电脑端获得更高的信息密度。

## 7. 功能需求

### 7.1 今日页

页面顶部显示当前日期，默认今天，可切换并补录过去日期。首屏摘要包括：

- 今日已记录嘌呤估算范围。
- 估算覆盖状态，例如“完整”“部分覆盖：2 项暂无参考值”。
- 今日饮品总量，其中纯净水和其他饮品分别显示。
- 最近一次血尿酸实测值及日期；没有记录时显示空状态。
- 四个主操作入口：`记录食物/菜谱`、`记录饮品`、`记录尿酸`、`记录治疗`。

今日页还显示“基于最近实测的参考建议”块：

- 有最近实测时，显示该值、WS/T 560—2017 的性别参考线和“需要复核”提示；例如 `535μmol/L` 只作为用户录入的实测事实，不被解释为诊断结果。
- 若用户已经被医生诊断痛风并正在接受降尿酸治疗，可以并列显示 ACR 2020 的 `<6mg/dL`（约 `360μmol/L`）治疗目标参考；该数字不适用于把单次高尿酸读数自动变成个人处方。
- 显示新鲜蔬菜 `≥500g/日` 与饮品容量 `≥2000mL/日` 的来源性一般食养参考。达到约 80% 时允许给页面内“接近参考量”提醒，达到后只显示记录进度；不把它们写成医学风险告警，也不自动推出膳食嘌呤的硬性每日安全上限。
- 未记录的日期不等于摄入量为 0；菜谱不会在没有明确配料拆分时被假装计入蔬菜克数。

下方按时间倒序显示当天记录。每条记录可编辑、删除；删除需要确认或短时间撤销。任何改动都立即重新计算摘要。

### 7.2 记录食物或菜谱

录入流程：

1. 在“食物 / 菜谱”之间切换。
2. 通过搜索下拉选择条目；可按分组筛选，也始终能看到“未分组”。
3. 选择克数模板或输入自定义克数。
4. 保存前预览该条嘌呤估算范围和数据来源状态。
5. 保存后更新当天汇总，并保留当时参考值快照。

默认份量模板：

- 食物：`50g`、`100g`、`150g`、`200g`。
- 菜谱：`100g`、`150g`、`200g`、`300g`。
- 两组模板均可在管理页调整；每次录入都可输入任意有效正数。

输入规则：

- 克数必须是有限正数，不接受 0、负数、`NaN` 或无穷值。
- 异常大的数值采用二次确认，而不是把产品阈值宣传成医学安全线。
- 同一天可多次记录同一条目；完全重复提交时提醒用户。

### 7.3 记录饮品

模块名称使用“饮品”，因为茶和咖啡不等同于纯水。

录入字段：

- 饮品选择器。
- 容量模板：`100mL`、`200mL`、`300mL`、`500mL`、`1000mL`。
- 自定义毫升数。
- 可选数量，例如 `500mL × 2`，最终规范化存储为 `1000mL`。

用户提出的九种饮品作为系统预置项：

1. 纯净水
2. 无糖苏打水
3. 淡绿茶
4. 无糖柠檬水
5. 黑咖啡/无糖咖啡
6. 无糖薏仁水
7. 无糖大麦茶
8. 无糖冬瓜茶
9. 无糖玉米须茶

这些条目放入“系统预置”分组。若界面以后使用“推荐”二字，必须同时显示“仅表示系统预置，不代表医学推荐”。同名饮品若含糖、奶、酒精或大量钠，应创建不同条目，不能沿用无糖版本。

统计规则：

- 显示饮品总量、纯净水量和按类型分组的量。
- 饮品只贡献容量；不自动赋予“降尿酸值”。
- 饮水目标由用户配置。只有在心、肾功能正常且没有专业人员限制饮水时，界面才可引用 `2000–3000mL/日` 的一般食养建议；无自定义目标时，记录页使用 `2000mL/日` 作为一般参考下限。
- 上述数字只能标为“来源性一般食养资料”。页面可以在记录达到约 80% 时提供进度/接近提示，但不能自动变成个人目标、医学达标判定或风险告警。用户不确定自身心肾功能或是否需要限液时，产品不引导其自行判断，应提示咨询医生或营养专业人员后再设置。

### 7.4 血尿酸实测记录

“记录尿酸”按钮位于今日页和统计页。该功能是低频记录，不要求每日填写。

录入流程：

1. 选择测量日期，并按需填写时间。
2. 输入数值和原始单位。
3. 保存前同时预览原始值与换算值，例如 `7.0mg/dL = 416.36μmol/L`；换算仅统一统计口径，不作医学解释。
4. 按需展开补充信息。
5. 保存成功后显示明确反馈，并立即更新今日页“最近一次实测”和统计页；保存失败时保留表单并允许重试。
6. 新建请求使用客户端唯一 ID 或幂等键，避免弱网重试产生重复测量。

必填字段：

- 测量日期，默认今天，可选择过去日期。
- 数值。
- 单位，默认 `μmol/L`，也支持 `mg/dL`。

折叠在“补充信息”中的可选字段：

- 测量时间；没有时间时只保存日期，不虚构中午 12 点。
- 空腹、非空腹或未知。
- 医院检验、体检、家用设备或其他来源。
- 检测机构或设备名称。
- 是否处于急性发作期。
- 检验报告上的参考下限、上限及单位。
- 备注。

系统同时保存原始输入和规范化值：

```text
μmol/L = mg/dL × 59.48
mg/dL = μmol/L ÷ 59.48
```

示例测试：`7.0 mg/dL` 应换算为 `416.36 μmol/L`，界面可按显示规则取整，数据库保留计算精度。

单位与统计规则：

- 数据库的规范统计单位固定为 `μmol/L`；趋势、差值、最小值、中位数和最大值均先使用规范值计算。
- 页面按用户当前首选单位统一显示整个图表和统计卡，不能在同一坐标轴混用两种单位。
- 记录详情始终保留并展示原始数值、原始单位；可同时显示换算值。
- 修改首选单位只改变显示，不修改历史原始数据。
- 检验报告参考范围必须保存其原始单位；参与图表参考线前，按同一换算版本转换为当前显示单位。
- CSV 同时导出 `value_original`、`unit_original`、`value_umol_l` 和换算版本，避免丢失原始报告信息。

其他规则：

- 数值必须大于 0；极端值提示复核，不直接删除真实报告数据。
- 不允许无提示地选择未来日期。
- 同一天允许多次测量。
- 参考线优先使用用户录入的检验报告范围；产品不以单一阈值自行作出诊断。
- [NICE 痛风指南](https://www.nice.org.uk/guidance/ng219/chapter/Recommendations)指出急性发作期间血尿酸可能较低，因此“是否处于发作期”有助于回看，但系统不据此诊断。

### 7.5 治疗记录

治疗记录页是独立的事实时间线，用于记录用户经历过或计划中的治疗过程节点。它不把多条记录合并成一个“疗程”，也不根据时间先后推断药物效果；例如同一天做检查、抹药和出现症状变化，应保存为三条独立记录，并在时间线上分别显示。

#### 7.5.1 页面与筛选

- 治疗记录作为一级页面，移动端底部导航和电脑端主导航都可直接进入。
- 页面顶部提供“新增治疗记录”按钮，以及日期范围筛选、事件类型筛选和关键词搜索。
- 类型筛选至少包含“全部、痛风发作、医院检查、口服药、外用药、症状变化、复诊计划、其他”。选择某一类型后，只显示该类型的时间线，并显示当前筛选结果数量。
- 默认显示全部时间线，按事件日期倒序；同一天内，有事件时间的记录按时间倒序，没有事件时间的记录排在有时间记录之后，并以创建时间作为稳定排序依据。
- 没有填写事件时间时，只显示“时间未填写”，不得用创建时间伪装事件发生时间。
- 时间线按日期分组，但每条记录仍是独立卡片；同一天的多条记录不能合并成一条摘要或覆盖彼此。
- 空状态要区分“没有任何治疗记录”和“当前筛选条件下没有记录”，并提供新增入口或清除筛选入口。

#### 7.5.2 新增、编辑和删除

- 日期和事件类型是唯一的必填字段；事件时间、标题、药品名称、检查结果、机构信息、症状描述、剂量、频次、起止日期、复诊日期和备注均为可选字段。
- 日期默认今天，可选择过去日期；“复诊计划”允许选择未来日期。其他类型选择未来日期时必须二次确认，不能无提示地把未来计划记录成已发生事实。
- 标题为空时使用事件类型作为默认标题；用户填写标题后，时间线优先显示用户标题。
- 新建和编辑使用同一表单。选择事件类型后，只展开该类型相关字段，通用字段始终保留，避免把不相关字段全部堆在一个表单中。
- 每条记录均支持编辑和软删除；删除前二次确认，删除后不再出现在普通时间线，但保留审计信息，不影响其他日期或其他类型记录。
- 新建请求使用客户端唯一 ID 或幂等键；幂等约束只防止同一次请求重复提交，不限制同一天添加多条内容相同或不同的事件。

#### 7.5.3 类型字段设计

| 事件类型 | 可选字段 | 时间线重点展示 |
| --- | --- | --- |
| 痛风发作 | 发作部位、严重程度 `0–10`、症状状态、症状描述 | 部位、严重程度、备注 |
| 医院检查 | 医院/机构、科室、医生、检查名称、报告结论、下次复诊日期 | 检查名称、结果摘要、机构 |
| 口服药 | 药品名称、剂量、剂量单位、频次、开始日期、结束日期、医嘱原文 | 药品名称、剂量/频次、起止日期 |
| 外用药 | 药品或产品名称、涂抹部位、频次、开始日期、结束日期、使用说明 | 产品名称、部位、频次 |
| 症状变化 | 变化状态（缓解/无明显变化/加重/其他）、严重程度、症状描述 | 变化状态、严重程度、备注 |
| 复诊计划 | 计划事项、医院/机构、科室、计划日期、备注 | 计划日期、计划事项 |
| 其他 | 事件名称、详细描述 | 事件名称、备注 |

“医院检查”允许添加 `0` 到多条检查结果行。每行可以填写检查项目、原始结果文本、可选数值、单位、报告参考范围和备注；原始结果文本优先保留，不自动解释、分级或转换为诊断结论。药物字段同样只保存用户填写的事实，不建立药品推荐库，不计算疗效，不生成开始、停用或调剂量建议。

#### 7.5.4 与其他页面的关系

- 今日页增加“记录治疗”快捷入口，并显示当天治疗节点数量；点击数量可进入治疗记录页并自动带上当天日期筛选。
- 历史页的每日摘要增加治疗记录数量；进入日期详情时可以看到该日期的治疗节点入口。
- 统计页 v1 不绘制“治疗后尿酸变化”或药物效果图，不计算相关性或因果结论。后续如增加探索性对照，必须明确显示样本量、缺测和“时间关联不等于疗效”的边界。
- 治疗记录与饮食、饮品、血尿酸记录相互独立；同一天可以同时存在多类记录，不能因为删除某一类记录而删除其他记录。

### 7.6 历史页

- 按日期倒序显示每日摘要。
- 每日摘要包含饮食估算范围、覆盖状态、饮品总量和记录条数。
- 点击日期进入完整详情，可编辑或删除过去记录。
- 支持按日期跳转。
- 未记录的日期显示为“无记录”，不能当作摄入量为 0。
- v1 使用列表和日期详情；日历热力图后置。

### 7.7 统计页

统计页分成三个独立区域，避免双坐标图暗示因果关系。

#### A. 血尿酸实测趋势

- 显示最新值、上一次值、两次测量间隔和差值。
- 显示记录数、最小值、中位数、最大值。
- 提供 `近 30 天`、`近 90 天`、`近 1 年`、`全部`和自定义范围。
- 按真实日期间距绘制点线图，不把缺测日期补为 0，也不生成每日插值。
- 0 条记录时显示添加入口；1 条时只显示单点并说明至少两条才有变化趋势。
- 图下提供可读的数据表。

#### B. 饮食与饮品趋势

- 每日嘌呤估算范围使用区间带或上下界柱形显示。
- 每日饮品总量、纯净水量和其他饮品量单独显示。
- 周/月汇总必须显示“实际有记录天数 / 区间总天数”。
- 未记录日不能参与平均值，也不能视为 0。

#### C. 同期记录对照

每次尿酸实测旁，可显示测量前 `1`、`3`、`7` 个完整日的：

- 嘌呤估算范围。
- 饮品总量和纯净水量。
- 数据覆盖率和实际记录天数。

固定提示语：

> 以下为同期记录，仅供自我观察；它不能证明某种食物或饮品导致本次血尿酸变化。

v1 不计算相关系数、食物排名式因果结论或未来尿酸预测。

### 7.8 资料库与管理页

管理页使用页签：

- 食物
- 菜谱
- 饮品
- 分组
- 份量模板
- 参考来源

所有资料列表支持搜索、分组筛选、新建、编辑和归档。

分组通用规则：

- 新建食物、菜谱、饮品时，分组均可为空，界面统一显示“未分组”。
- 食物、菜谱和饮品的分组彼此独立。
- 分组可改名、排序。
- 删除一个仍有内容的分组时，只把内容移动到“未分组”或用户指定的其他组，不级联删除内容。
- 被历史记录引用的条目采用归档/软删除：不能再新选，但历史仍可读。
- 系统预置项与用户自建项使用不同标记。编辑系统项时创建用户副本，不覆盖原始数据。

### 7.9 设置与数据页

- 修改食物、菜谱和饮品份量模板。
- 设置饮水目标及适用条件提示。
- 设置默认血尿酸单位。
- 查看估算方法、数据来源和免责声明。
- 修改共享访问口令。
- 查看当前可信设备，撤销本设备、指定设备或全部设备的长期凭证。
- 创建服务器备份、下载完整导出、预览恢复、执行迁移。
- 导出血尿酸和每日汇总 CSV，便于用户自行查看。
- 删除个人数据前进行强确认并生成可选备份。

## 8. 标准食物参考库

### 8.1 推荐食物分组

1. 主食/谷薯
2. 蔬菜
3. 水果
4. 豆类及豆制品
5. 菌菇
6. 畜禽肉
7. 动物内脏
8. 水产/海鲜
9. 蛋奶
10. 坚果/种子
11. 汤、浓汁及火锅汤底
12. 零食、调味及其他
13. 未分组（数据库中为 `NULL`，不是一个真实分组）

分组只用于查找，不能直接代表风险。例如，植物性食物不能仅因总嘌呤较高就与动物内脏作相同解释。

### 8.2 每项食物的数据字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 名称 | 是 | 明确到可辨认食物 |
| 别名 | 否 | 用于搜索，不用于合并不同品种 |
| 分组 | 否 | 为空即未分组 |
| 状态 | 是 | 生、熟、干制、罐装、汤汁等 |
| 计量基准 | 是 | 食物为每 100g 可食部；菜谱为每 100g 成品；饮品参考数据可为每 100mL |
| 嘌呤下限 | 是或未知 | `mg/100g`；允许与上限相等 |
| 嘌呤均值 | 否 | 数据源提供时保存 |
| 嘌呤上限 | 是或未知 | `mg/100g`；不得小于下限 |
| 范围类型 | 是 | 单点、样本最小最大、资料报告范围、用户估计 |
| 数据来源 | 是 | 标题、发布方、版本、URL、页码/表号 |
| 样本量/误差 | 否 | 数据源提供时原样保存 |
| 复核状态 | 是 | `PREPARED`、`VERIFIED`、停用 |
| 备注 | 否 | 可食部、品种、烹饪和映射限制 |

关键约束：`0 ≤ 下限 ≤ 均值 ≤ 上限`；均值缺失时仍要求 `下限 ≤ 上限`。`下限 = 上限` 表示约值，不表示绝对精确。未知使用 `NULL`，不使用 0。

### 8.3 嘌呤含量标签

若采用《成人高尿酸血症与痛风食养指南（2024年版）》的分档口径，建议将分类方案本身版本化为 `NHC-2024`：

| 每 100g 总嘌呤 | 显示标签 |
| --- | --- |
| `<30mg` | 低 |
| `30–<75mg` | 较低 |
| `75–<150mg` | 较高 |
| `≥150mg` | 高 |

范围跨档时显示跨档结果，不强行压成一个精确标签。该标签只描述参考表数值，不等于“吃了就会升高多少血尿酸”。实现前应再次逐页核对正式指南中的单位、边界和适用范围。

### 8.4 中国食物种子数据示例

下表只演示数据结构。数值来自 WS/T 560—2017 附录的 `mg/kg`，此处除以 10 转为 `mg/100g`。源文件仅给出单点时，产品应显示“约值”，不得伪造范围。

| 分组 | 食物及状态 | 约值（mg/100g） | 来源状态 |
| --- | --- | ---: | --- |
| 主食/谷薯 | 普通大米 | 34.67 | 待逐项人工复核后发布 |
| 主食/谷薯 | 小米 | 20.06 | 待逐项人工复核后发布 |
| 主食/谷薯 | 甘薯 | 18.62 | 待逐项人工复核后发布 |
| 蔬菜 | 红萝卜 | 13.23 | 待逐项人工复核后发布 |
| 蔬菜 | 白萝卜 | 10.98 | 待逐项人工复核后发布 |
| 水果 | 菠萝 | 11.48 | 待逐项人工复核后发布 |
| 水果 | 柚子 | 8.37 | 待逐项人工复核后发布 |
| 水果 | 橘子 | 4.13 | 待逐项人工复核后发布 |
| 豆类及豆制品 | 豆浆 | 63.17 | 待逐项人工复核后发布 |
| 豆类及豆制品 | 豆腐块 | 68.63 | 待逐项人工复核后发布 |
| 畜禽肉 | 鸡胸肉 | 207.97 | 待逐项人工复核后发布 |
| 畜禽肉 | 猪肉（后臀尖） | 137.84 | 待逐项人工复核后发布 |
| 动物内脏 | 鸡肝 | 317.00 | 待逐项人工复核后发布 |
| 水产/海鲜 | 扇贝 | 193.44 | 待逐项人工复核后发布 |
| 水产/海鲜 | 基围虾 | 187.40 | 待逐项人工复核后发布 |
| 水产/海鲜 | 河蟹 | 147.00 | 待逐项人工复核后发布 |
| 水产/海鲜 | 草鱼 | 134.44 | 待逐项人工复核后发布 |
| 菌菇/藻类 | 紫菜（干） | 415.34 | 待逐项人工复核后发布 |

当前候选实现已在上述 WS/T 560 种子之外补充圆白菜、小白菜、西兰花、菜花、黄瓜、番茄、茄子、生菜、菠菜、豆芽、玉米、马铃薯、乌冬面、荞麦面、鸡蛋、牛奶、香蕉和草莓等常见条目，并提供番茄炒蛋、清炒西兰花、凉拌黄瓜、胡萝卜炒鸡胸、番茄豆腐汤、小米粥、玉米米饭和土豆烧茄子等按配料计算的示例菜谱。补充值来自 USDA/ODS Release 2.0 的四种嘌呤碱基总量；该口径与 WS/T 560 不完全相同，所有补充条目和系统示例菜谱保持 `PREPARED`，用户应按实际品种、生熟/干湿状态和成品重量复核后再使用。

完整种子库不应直接从零散网页拼接。建议的数据建设顺序：

1. 以国家卫健委资料建立中国常见食物的名称和分类骨架。
2. 使用 USDA/NIH-ODS 2025 数据补充带最小值、最大值、均值和样本信息的条目。
3. 使用 Kaneko 等资料补充亚洲常见食物及嘌呤碱基信息，但批量内置前核对转载/使用条件。
4. 对中文名称、物种、部位、生熟、干湿和单位进行人工映射。
5. 每条记录保留来源版本和原始行标识；只有完成复核的记录才能标记 `VERIFIED` 并作为默认计算数据。

## 9. 菜谱库

### 9.1 推荐菜谱分组

- 家常菜
- 素菜
- 畜禽肉菜
- 水产菜
- 汤羹
- 主食/面点
- 外食/其他
- 未分组

### 9.2 两种创建方式

#### 方式 A：按配料计算

适合西红柿炒鸡蛋、宫保鸡丁等用户知道配料和成品重量的菜：

- 选择多个已发布食物版本。
- 填写每种原料的克数。
- 填写烹饪后的成品总重量。
- 系统计算整份和每 100g 的嘌呤范围。

如果没有成品重量，只能显示“整份原料估算”，不能声称已经得到可靠的每 100g 成品值。煮、炖、弃汤、吸水和失水会改变结果；应优先使用与实际烹饪状态相符的食物参考项。

#### 方式 B：手工范围

适合外卖、餐馆菜或已有可信菜品资料但不知道配方的情况：

- 表单字段明确写为“嘌呤下限（mg/100g 成品）”和“嘌呤上限（mg/100g 成品）”。
- 必须填写来源或“用户估计”说明。
- 页面明确显示“手工估计”，不与配料计算结果混淆。

v1 不允许菜谱嵌套菜谱，以避免循环引用和难以解释的版本变化。

## 10. 计算规则

### 10.1 单项食物

食用量为 `q` 克，参考基准为 `b` 克：

```text
entry_low  = q / b × purine_low
entry_high = q / b × purine_high
```

### 10.2 按配料计算菜谱

```text
recipe_total_low  = Σ(ingredient_g / basis_g × food_low)
recipe_total_high = Σ(ingredient_g / basis_g × food_high)

recipe_low_per_100g  = recipe_total_low / final_yield_g × 100
recipe_high_per_100g = recipe_total_high / final_yield_g × 100
```

食用该菜谱 `q` 克：

```text
entry_low  = q / 100 × recipe_low_per_100g
entry_high = q / 100 × recipe_high_per_100g
```

### 10.3 当日汇总

```text
daily_low  = Σ known_entry_low
daily_high = Σ known_entry_high
```

若存在未知项，仍汇总已知项，但必须标记“部分覆盖”并显示未知条目数。不能把未知条目当作 0。

### 10.4 精度与快照

- 服务端使用十进制定点数计算，不使用二进制浮点数作为持久真值。
- 计算中保留至少 3 位小数，展示时统一舍入。
- 客户端只提交条目 ID、数量和日期；服务端负责计算。
- 每条饮食记录保存名称、分组、参考范围、计算版本和本次结果快照。
- 参考表、菜谱或分组以后改变时，历史记录不自动重算。

## 11. 数据模型概览

| 实体 | 关键内容 |
| --- | --- |
| `app_settings` | 单数据空间的时区、默认单位、饮水目标和显示设置 |
| `trusted_device_sessions` | 随机凭证哈希、会话世代、创建时间、到期时间、最后使用时间、设备标签和撤销时间 |
| `reference_sources` | 标题、发布方、版本、URL、文件哈希、使用说明 |
| `food_groups` / `foods` / `food_versions` | 可空分组、名称、状态、不可变参考版本 |
| `recipe_groups` / `recipes` / `recipe_versions` | 两种计算方式、成品重量、不可变版本 |
| `recipe_ingredients` | 菜谱版本引用的具体食物版本及克数 |
| `beverage_groups` / `beverages` | 可空分组、是否纯水、是否含糖、备注 |
| `portion_presets` | 类型、数值、单位、排序 |
| `diet_entries` | 日期、克数、食物或菜谱版本、录入时快照 |
| `beverage_entries` | 日期、饮品、毫升数、名称快照 |
| `urate_measurements` | 原始值、单位、规范值、日期/可选时间、检测条件 |
| `treatment_events` | 客户端幂等 ID、事件日期/可选时间、类型、标题、通用备注、类型字段、创建/更新时间和软删除时间 |
| `treatment_event_results` | 检查事件的项目、原始结果文本、可选数值/单位/参考范围、备注和排序 |
| `backup_records` | 备份时间、类型、版本、校验和、恢复验证状态 |

重要约束：

- 所有业务表属于同一个数据空间，不保存 `user_id`、账户或角色字段。
- 共享口令哈希放在服务器私密配置中，不放进业务数据库；可信设备表只保存随机凭证的哈希。
- 食物、菜谱和饮品的 `group_id` 均允许为空。
- 饮食记录只能引用食物或菜谱之一。
- 版本发布后不可原地修改；新资料创建新版本。
- 软删除后的资料不能再新选，但历史引用保持有效。
- 日期汇总使用用户本地日期；服务器时间戳使用 UTC，并保存时区上下文。
- 治疗记录只要求事件日期和事件类型；同一日期允许任意多条记录，不以日期或类型建立唯一约束。
- 治疗记录的事件时间是用户本地 `HH:mm`，可以为空；没有时间时不从 `created_at` 推导展示时间。
- 检查结果使用独立子表支持一条检查事件对应多项结果；结果原文和单位按用户输入保存，不参与自动医学解释或诊断计算。
- 治疗记录中的药品、机构、医生和报告内容均作为用户输入快照保存，不建立会自动更新或推荐的外部主数据表。

## 12. 技术架构与部署

### 12.1 推荐形态

- 功能模块化的单体 Web 应用，模块包括 `access_gate`、`food`、`recipe`、`beverage`、`entry`、`measurement`、`statistics`、`settings`、`backup`。
- 前后端同源部署；浏览器不直接访问数据库。
- 所有计算、访问凭证校验和导入校验在服务端执行。
- Docker Compose 管理应用和 HTTPS 反向代理。
- SQLite 运行在单应用实例中，开启适合并发读取的模式，并使用在线备份机制。
- 数据目录挂载到宿主机或命名卷，不能写进容器临时层。

### 12.2 API 边界

至少提供以下资源：

- 访问状态、口令验证、当前设备撤销和全部设备撤销。
- 应用设置和服务健康状态。
- 食物/菜谱/饮品及各自分组的增删改查和归档。
- 菜谱计算预览与版本发布。
- 饮食、饮品、尿酸实测记录的增删改查。
- 治疗记录及检查结果的增删改查、日期范围查询和事件类型筛选。
- 指定日期的汇总和指定区间的统计。
- 完整导出、备份、恢复预览和恢复执行。

治疗记录接口约定：

- `GET /api/treatment-events?from=YYYY-MM-DD&to=YYYY-MM-DD&type=...&q=...` 返回按日期倒序的事件及其检查结果；`type` 和 `q` 可省略。
- `POST /api/treatment-events` 新建事件，至少接收 `clientId`、`eventDate` 和 `eventType`；空的可选字段按 `null` 保存。
- `PUT /api/treatment-events/:id` 编辑事件及其结果行，使用同一事件 ID，不创建隐式副本。
- `DELETE /api/treatment-events/:id` 软删除事件及其检查结果，不物理删除审计记录。
- `GET /api/day` 返回指定日期的治疗节点数量和节点摘要；`GET /api/history` 的每日摘要包含治疗节点数量。
- 所有治疗记录写接口使用现有认证、CSRF、幂等和审计边界；查询接口不得在未认证时泄露敏感内容。

写接口使用幂等键；管理数据编辑使用版本号或 ETag，避免重复提交和并发覆盖。

## 13. 持久化、备份与迁移

### 13.1 持久化要求

- SQLite 数据库位于明确的服务器持久化目录，例如容器内 `/app/data/app.db` 对应宿主机或命名卷。
- 数据目录、备份目录和密钥均不进入 Git，也不打包进应用镜像。
- 应用启动时执行有版本号的数据库迁移；升级前自动创建备份。
- 容器删除、重建或升级后，挂载卷中的数据必须保持不变。

### 13.2 两层备份

1. **数据库安全快照**：用于同版本快速恢复。必须使用 SQLite 在线备份 API、`VACUUM INTO` 或停机快照，不能在写入过程中简单复制数据库文件。
2. **应用级完整导出**：版本化 JSON/ZIP，用于跨版本和跨服务器迁移。

完整导出至少包含：

- `manifest.json`：导出格式版本、应用版本、数据库模式版本、创建时间、时区和校验和。
- 应用设置、三个资料库及分组、来源、菜谱版本和配料。
- 份量模板。
- 饮食、饮品、血尿酸实测和治疗记录历史，包括治疗记录对应的检查结果行。
- 不包含共享口令及其哈希、可信设备凭证、服务器签名密钥、备份加密密钥或其他私密部署配置。

CSV 仅作为便于阅读的附加导出，不代替完整备份。

### 13.3 自动备份策略

推荐默认：

- 每日 1 次加密备份，保留最近 7 份。
- 每周保留 1 份，保留最近 4 份。
- 每月保留 1 份，保留最近 12 份。
- 至少一份复制到不同磁盘或不同服务器；同一磁盘上的副本不能视为完整备份。
- 备份密钥与备份文件分开保存。

系统记录最近一次成功备份、文件大小、校验和和恢复验证时间。只生成文件但没有恢复验证，不能标记为“备份已验证”。

执行与审计约定：

- 由宿主机计划任务调用应用提供的单次备份命令；命令必须以退出码表示成功或失败，避免依赖浏览器保持在线。
- 本地快照成功后再复制到配置的异盘或异机位置，并在目标位置重新校验文件哈希；两个阶段分别记录状态。
- 备份使用带完整性校验的加密格式，文件头记录格式版本和密钥版本；密钥轮换后至少保留仍在保留期内备份所需的旧密钥。
- 失败会写入结构化日志，并在用户下次打开管理页时显示持续告警，直到后续备份成功或用户明确处理。
- 最低验收证据包括任务开始/结束时间、退出码、文件大小、两端校验和、异地复制结果，以及一次空环境恢复报告。

### 13.4 恢复与迁移流程

1. 用户选择备份文件。
2. 服务端验证格式版本、校验和、模式兼容性和基本数据约束。
3. 页面预览将恢复的条目数量、日期范围和目标版本。
4. 恢复前自动备份当前数据。
5. v1 采用“完整替换恢复”，不做语义不明的自动合并。
6. 在临时数据库中导入和校验成功后再原子切换。
7. 无论恢复可移植导出还是 SQLite 数据库快照，都在切换前清空全部 `trusted_device_sessions`，并轮换服务器会话世代；历史快照中的旧会话不得恢复生效。
8. 新版导出增加治疗记录及检查结果表；导入旧版导出时，缺少这两张表按空数据处理，不得影响原有资料和历史记录恢复。
9. 重新计算汇总，并比对导出前后的资料条目数、饮食/饮品/尿酸记录数、治疗记录数、检查结果行数和关键统计。

目标服务器必须先通过私密部署配置设置共享口令哈希和新的服务器签名密钥，再执行恢复。恢复完成后仍使用同一个数据空间，不创建账户或映射数据所有者；出于安全性，迁移后所有设备都需要重新输入一次共享口令。

新服务器迁移只有在以下结果全部一致后才算成功：新设备只需输入共享口令且不需要用户名、通过后可直接记录、资料库数量一致、历史记录一致、尿酸实测一致、每日汇总一致、统计页可打开、备份可再次生成。

## 14. 安全与隐私

v1 不建设账户系统，只使用单一共享口令和长期可信设备凭证：

- 全站 HTTPS；数据库端口不暴露公网。
- 未持有有效可信设备凭证的浏览器只能访问口令验证页和必要的静态资源，不能读取、修改、导出或恢复业务数据。
- 口令验证接口限速，并对连续失败施加渐进等待；日志不记录输入内容。
- 成功验证后生成至少 256 位随机凭证；Cookie 使用 `HttpOnly`、`Secure` 和合适的 `SameSite`，数据库只保存凭证哈希。
- 每个受保护请求都必须在服务端确认凭证记录存在、未撤销、未过期且会话世代与当前配置一致；任一条件不满足立即返回未授权并清除 Cookie。
- 修改共享口令时立即撤销全部可信设备凭证；服务端管理命令也可在无法进入网页时重设口令并撤销全部设备。
- API 默认只允许同源浏览器请求，关闭宽泛 CORS；所有写接口使用 CSRF 防护、JSON 和自定义请求头。
- 参数化查询、输入校验、内容安全策略和基础审计日志。
- 日志不记录口令、Cookie、完整请求体或具体血尿酸值。
- 治疗记录中的药品名称、剂量、检查结果、机构和症状描述属于敏感个人健康信息；日志和错误响应不得写入这些字段的完整内容。
- 共享口令哈希、服务器签名密钥和备份加密密钥只放私密配置，不进入源码、Git 或可移植导出。
- 默认不接入第三方广告、用户行为分析或外部健康数据服务。
- 口令成功/失败、设备撤销、下载备份、恢复和删除全部数据写入审计记录，但不写入秘密内容。
- 治疗记录的新增、编辑和删除写入审计事件；审计元数据只保留事件 ID、事件类型和操作结果，不复制药品、报告或症状正文。
- 恢复、删除全部数据等破坏性操作仍需输入明确确认短语，避免已信任设备上的误触。

## 15. 异常与边界处理

- g 和 mL 没有可靠密度时禁止自动换算。
- 同名但生熟、干湿、品种或部位不同的食物必须分项。
- 菜谱成品重量必须大于 0；无法计算每 100g 的草稿不能进入正式选择列表。
- 用户自建且无参考值的食物可以记录，但只计入“未知项”。
- 未记录某日不等于当天没有进食或饮水。
- 尿酸实测只有日期时，不伪造具体时间。
- 尿酸值突然变化只显示差值，不自动归因到最近一餐。
- 治疗记录只有日期时，不伪造事件时间；同一天的多条事件必须逐条保留并可分别编辑、删除。
- 治疗记录的可选字段为空时正常保存；时间线显示已填写的信息，不用占位文本制造“未知结果”或“未使用药物”等事实。
- 检查结果可以是文字、数值或两者同时存在；缺少单位、参考范围或报告结论时仍保留原始记录，不自动补全。
- 只有“复诊计划”默认允许未来日期；其他类型的未来日期需要明确二次确认。
- 删除分组不删除内容；归档资料不破坏历史。
- 还原旧备份时不允许静默丢弃新版本无法识别的字段。

## 16. v1 验收标准

### 16.1 录入与计算

- [ ] 食物、菜谱、饮品均可在无分组状态下创建、编辑和使用。
- [ ] 搜索选择器能按名称、别名和分组查找，手机单手可完成主要录入。
- [ ] 录入 `150g`、参考范围 `20–30mg/100g` 的食物，结果为 `30–45mg`。
- [ ] 菜谱整份、每 100g 和实际食用量三层计算一致。
- [ ] 缺少参考值的条目不会按 0 计算，并触发“部分覆盖”。
- [ ] `500mL × 2` 正确汇总为 `1000mL`。
- [ ] 保存、编辑、删除后，当天汇总立即一致。
- [ ] 修改参考食物、菜谱或分组后，过去记录的快照和汇总不变化。
- [ ] 弱网重试或连续点击不会创建重复记录。

### 16.2 血尿酸与统计

- [ ] 可选择过去日期录入血尿酸值；可编辑、删除。
- [ ] `mg/dL` 与 `μmol/L` 双向换算通过固定测试。
- [ ] 混合录入两种单位时，趋势和聚合统计先按 `μmol/L` 计算，再统一显示为用户首选单位；记录详情仍保留原始值。
- [ ] 尿酸保存前有换算预览，保存成功后今日摘要和统计立即更新；弱网重试不重复建项。
- [ ] 仅日期记录不会产生伪造时间。
- [ ] 0 条、1 条和多条测量都有正确空状态或趋势表现。
- [ ] 趋势按真实日期间距绘制，不插值、不把缺测日当 0。
- [ ] 血尿酸、饮食和饮品使用独立图表。
- [ ] 所有同期展示都带有“不能证明因果”的提示。

### 16.3 治疗记录

- [ ] 治疗记录作为独立一级页面，可从电脑端和 `360px` 手机端导航进入。
- [ ] 只填写日期和类型即可保存；标题、时间、药物、检查、症状和备注等可选字段为空时不报错。
- [ ] 同一天连续添加“医院检查、外用药、症状变化”三条记录后，时间线上显示三张独立卡片，互不覆盖或合并。
- [ ] 填写事件时间时按日期和时间排序；未填写时间时显示“时间未填写”，不显示伪造时间。
- [ ] 可按全部、单一事件类型、日期范围和关键词筛选；筛选结果数量、空状态和清除筛选入口正确。
- [ ] 医院检查可以添加多条检查结果；每条结果保留原始文字，并正确显示可选数值、单位和参考范围。
- [ ] 编辑后只更新目标事件；删除需要确认并只软删除目标事件，同日其他事件和饮食/饮品/尿酸记录不受影响。
- [ ] 新建请求幂等重试不会重复创建，但同一天不同事件仍可正常保存。
- [ ] 未来日期只允许复诊计划直接保存，其他事件会出现明确二次确认。
- [ ] 治疗记录不生成疗效、因果、诊断或用药调整结论；页面边界提示可见。

### 16.4 移动端与电脑端

- [ ] `360px` 宽度下无关键内容溢出，主要按钮可触控，表单不依赖悬停。
- [ ] 手机端选择、输入、自定义数量、修改和删除均可完成。
- [ ] 统计图在手机上可读，并有等价数据表。
- [ ] 电脑端可完成全部功能，管理和备份操作信息密度合理。

### 16.5 持久化、安全与迁移

- [ ] 删除并重建应用容器后，持久化卷中的数据仍完整。
- [ ] 新浏览器第一次访问只要求共享口令，不要求用户名、注册或创建账户。
- [ ] 正确输入一次后生成长期可信设备凭证；刷新、关闭再打开及服务器普通重启后均可直接记录。
- [ ] 新设备、无痕窗口、已清除 Cookie、过期或被撤销的设备必须重新输入口令。
- [ ] 无有效凭证时，页面和 API 均不能读取、修改、导出或恢复业务数据。
- [ ] 撤销某设备后，该设备持有的旧 Cookie 在下一次页面或 API 请求时立即失效，不能继续使用到原到期日。
- [ ] 修改或通过服务器命令重设共享口令后，全部旧设备凭证立即失效。
- [ ] 浏览器、日志、数据库导出和源码中不存在共享口令明文；业务数据库不保存口令哈希。
- [ ] 数据库、备份和服务器密钥不进入 Git 或应用日志。
- [ ] 完整导出可在空环境恢复，资料、记录、单位、版本引用和汇总一致。
- [ ] 完整导出和恢复包含治疗记录及检查结果；旧版导出缺少新增表时按空数据兼容恢复。
- [ ] 治疗记录中的敏感正文不出现在日志、审计元数据、源码或共享口令配置中。
- [ ] 空环境恢复前后均不创建账户；目标服务器配置共享口令后即可恢复，恢复后各设备只需重新验证一次。
- [ ] 恢复任意 SQLite 快照后，快照中的可信设备记录不会复活；所有设备均需重新验证一次。
- [ ] 恢复失败不会破坏原数据库。
- [ ] 至少完成一次从旧服务器备份到新服务器的真实迁移演练。
- [ ] 自动备份只有在成功恢复验证后才标记 `VERIFIED`。
- [ ] 自动任务、异机/异盘复制、加密密钥版本和失败告警均留下可审计证据。

## 17. 交付状态定义

- `PREPARED`：文档、页面、参考数据草表或 AI 生成内容已经准备；不代表代码完成或医学数据已核验。
- `VERIFIED`：参考条目已逐项核对来源；计算、共享口令与可信设备访问控制、目标服务器部署、备份与恢复经过实际测试。软件测试通过仍不等于医学预测有效。
- `ACCEPTED`：用户在实际手机、电脑和目标服务器上完成验收，并接受交互和数据边界。
- 任何“临床有效”“可预测血尿酸”的声明都需要独立专业验证，不能由普通功能验收替代。

当前文档仅达到 `PREPARED`。

## 18. 建议实施顺序与决策门

### G0：确认产品口径

- 接受“估算膳食嘌呤摄入范围”，不再称“预计血尿酸升高值”。
- 接受饮品不抵扣嘌呤负荷。
- 接受单数据空间、无账户、一个共享口令和长期可信设备凭证，以及系统预置饮品、两种菜谱模式和默认份量模板。

### G1：参考库准备

- 建立来源登记表和数据版本规则。
- 制作首批中国常见食物种子数据。
- 人工核对名称、单位、生熟/干湿状态和来源页码。
- 将已核对条目标记为 `VERIFIED`，未核对条目不能默认参与正式计算。

### G2：移动端录入原型

- 先验证今日页、搜索选择器、份量模板、自定义输入和当天汇总。
- 用实际手机测试单手录入和弱网重试。

### G3：完整 v1

- 完成资料管理、历史、尿酸记录、治疗记录时间线、统计、设置和数据快照。
- 完成治疗记录的同日多条、日期/类型筛选、可选字段、检查多结果、编辑删除和旧导出兼容。
- 完成服务端计算测试和移动/电脑端回归。

### G4：服务器、备份与迁移

- 在目标服务器通过 HTTPS 部署。
- 验证持久化卷、自动备份、完整导出和空环境恢复。
- 完成一次实际迁移演练并记录校验结果。

### G5：用户验收

- 用户在主要手机和偶用电脑上完成核心路径。
- 用户确认术语不会被理解为医学预测。
- 只有此后，v1 才可标记 `ACCEPTED`。

## 19. 后续候选功能

以下内容不进入 v1，待真实使用反馈后再决定：

- 收藏、最近使用排序和一键重复昨天记录。
- 体重、运动、睡眠等非治疗扩展事件记录。
- 检验报告图片附件、OCR 和结构化报告导入。
- 服药提醒、疗程管理、药品目录和医生共享功能。
- PWA 安装与离线草稿。
- 日历热力图和更复杂报表。
- 经统计与医学评审后的探索性个体模型。

即使未来建立个体模型，也必须显示样本量、缺测率、误差、预测区间和混杂因素，且不能自动生成治疗建议。
