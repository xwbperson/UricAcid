# UricAcid 实现与验收状态

更新时间：2026-08-24

## 已实现并在本工作区验证

- Node.js + Express + SQLite 单体应用骨架、版本化初始化和持久化数据目录。
- 共享口令哈希、长期可信设备 Cookie、服务端凭证查询、撤销、过期、会话世代和 CSRF。
- 食物、菜谱、饮品、分组、份量模板、来源登记、历史记录、尿酸实测、统计和设置接口。
- 膳食嘌呤范围计算、未知值/部分覆盖、菜谱配料计算、手工范围、历史快照、500mL × 2 归一化。
- μmol/L 与 mg/dL 固定换算；统计使用规范单位，详情保留原始输入。
- JSON/ZIP 可移植导出、SQLite 在线快照、恢复预览/完整替换恢复；恢复会清除可信设备会话。
- 手机优先界面：今日、历史、统计、管理四个入口；统计图下有数据表和同期边界提示。
- Dockerfile、Compose 持久化卷和一次性备份命令。

## 自动化证据

- `npm run build`：通过。
- `npm test`：7/7 通过，包含 `150g × 20–30mg/100g = 30–45mg`、未知值不归零、菜谱三层计算、尿酸换算、认证/CSRF、幂等写入、饮品汇总和口令修改撤销。
- Playwright Chromium 360px 烟测：通过，覆盖口令门、食物、饮品、7mg/dL 预览、统计和管理页；截图为 `test-artifacts/mobile-manage.png`。

## 尚不能声称已完成的验收

- 规格文档中的首批食物数值仍是 `PREPARED` 示例种子，尚未逐项人工核对正式来源的名称、品种、生熟/干湿状态、页码和使用条件，不能标为 `VERIFIED`。
- 当前工作区没有真实目标服务器，因此尚未证明 HTTPS 反向代理、删除/重建容器后的持久化、异盘/异机复制、空环境恢复和真实迁移结果。
- 尚未完成用户在实际主要手机、偶用电脑和目标服务器上的 `ACCEPTED` 验收。
- 当前快照命令记录本地快照和校验和；自动计划任务、异机复制、备份密钥分离和恢复验证需要在部署环境配置并留下审计记录。

因此本仓库可以称为“已实现、已自动化验证的 v1 候选实现”，不能把项目状态从规格定义的 `PREPARED` 直接改成 `VERIFIED` 或 `ACCEPTED`。
