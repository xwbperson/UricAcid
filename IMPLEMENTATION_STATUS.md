# UricAcid 实现与验收状态

更新时间：2026-08-26

## 已实现并在本工作区验证

以下能力已在本地隔离数据或自动化测试中验证。

- Node.js + Express + SQLite 单体应用骨架、版本化初始化和持久化数据目录。
- 共享口令哈希、长期可信设备 Cookie、服务端凭证查询、撤销、过期、会话世代和 CSRF。
- 失效设备 Cookie 清理、登录限速、审计事件、CSP/安全响应头和不记录秘密的失败审计。
- 食物、菜谱、饮品、分组、份量模板、来源登记、历史记录、尿酸实测、统计和设置接口。
- 膳食嘌呤范围计算、未知值/部分覆盖、菜谱配料计算、手工范围、历史快照、500mL × 2 归一化。
- 未完成成品重量/每 100g 范围的菜谱不能进入正式记录；导入遇到未知字段会拒绝，不静默丢弃。
- μmol/L 与 mg/dL 固定换算；统计使用规范单位，详情保留原始输入。
- JSON 可恢复导出和 ZIP 归档导出（含应用/模式版本和 SHA-256 校验清单）、尿酸/每日汇总 CSV、SQLite 在线快照、AES-256-GCM 可选加密、异盘复制哈希校验、恢复预览/完整替换恢复；网页恢复会校验清单哈希并清除可信设备会话。
- 数据库模式迁移、持久化重开、删除个人记录前备份、SQLite 快照恢复与 `VERIFIED` 恢复证据命令。
- 手机优先界面：今日、治疗、历史、统计、管理五个入口；高频记录与时间线优先，医学说明、治疗筛选和低频设置渐进展开；弹窗支持焦点管理、标签关联和粘性保存区；统计图下有数据表和同期边界提示。
- 管理页按食物、菜谱、饮品标签分别管理独立分组；条目支持未分组，删除分组只会将内容移到未分组。
- 管理资料支持按当前标签页的分组筛选；桌面端资料列表和分组/来源面板分别滚动，移动端恢复为页面自然滚动。
- 食物、菜谱、饮品参考库条目支持从可选库删除（软归档并保留历史快照）；每日饮食、饮品和血尿酸记录支持逐条软删除。
- 参考库已补充常见蔬菜、主食、水果、蛋奶和 8 个示例菜谱；食物来源登记包含 WS/T 560—2017、国家卫健委 2024 食养指南、USDA/NIH-ODS 2025 数据库，新增数据仍为 `PREPARED`。
- 今日记录页已加入最近一次尿酸实测复核建议、蔬菜 `≥500g/日` 与饮品容量 `≥2000mL/日` 的来源性参考，以及接近参考量的页面内提醒；不生成个人血尿酸预测、诊断或用药建议。
- 治疗记录已作为独立一级时间线实现：支持同日多条医院检查/用药/症状节点、日期/类型/关键词筛选、检查多结果、可选字段、未来日期确认、编辑和软删除；今日页与历史页同步显示治疗节点数量，数据进入 JSON/ZIP 导出、恢复和旧导出兼容路径。
- Dockerfile、Compose 持久化卷和一次性备份命令。

## 自动化证据

以下结果来自 2026-08-26 的最终回归。

- `npm run build`：通过。
- `npm test`：通过，13/13 服务端测试；覆盖双标签 CSRF 稳定性、恢复哈希篡改拒绝、CSV 公式转义、蔬菜分组 ID、归档饮品历史编辑，以及治疗记录同日独立、多结果、筛选、编辑/软删除、导出恢复和旧导出兼容。
- `node --check public/app.js`：通过。
- `npm run test:coverage`：通过；语句 90.86%，分支 70.52%，前端不在该覆盖率中。
- `npm audit --registry=https://registry.npmjs.org`：0 个已知漏洞。
- Playwright Chromium 烟测：通过，覆盖 360px 移动端和 1280px 桌面端主要流程，并检查连续提交只新增一条、弹窗初始/恢复焦点、动态表单标签、两个管理面板独立滚动、无横向溢出和控制台/页面错误。另以 303px 模拟电脑手机模式扣除非覆盖式纵向滚动条后的有效宽度，确认整页不再横向滚动；390×844 和 412×915 隔离数据库检查今日、饮食弹窗、治疗、统计和设置均通过。
- 本机 Docker CLI 不可用，因此未在本机执行镜像构建、Compose 启停或容器重建验收。

## 尚不能声称已完成的验收

以下事项需要真实设备、目标服务器或人工资料核对，不能由本地自动化替代。

- 规格文档和当前补充的常见食物/菜谱数值仍是 `PREPARED` 示例种子，尚未逐项人工核对正式来源的名称、品种、生熟/干湿状态、页码和使用条件，不能标为 `VERIFIED`。
- 当前工作区没有真实目标服务器，因此尚未证明 HTTPS 反向代理、删除/重建容器后的持久化、异盘/异机复制、空环境恢复和真实迁移结果。
- 尚未完成用户在实际主要手机、偶用电脑和目标服务器上的 `ACCEPTED` 验收。
- 当前工作区只提供可被宿主机计划任务调用的单次备份命令；每日/每周/月度任务、异机目标、生产密钥分离和空环境恢复报告仍需在部署环境配置并留下审计记录。
- 本地自动化验证的是候选实现和临时数据库；尚未把任何真实目标服务器快照标为正式 `VERIFIED`。

因此本仓库可以称为“已实现、已自动化验证的 v1 候选实现”，不能把项目状态从规格定义的 `PREPARED` 直接改成 `VERIFIED` 或 `ACCEPTED`。
